version: "2"
services:
  node:
    image: "node:16"
    user: "node"
    working_dir: /var/www/html
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/var/www/html
    expose:
      - "80"
    command: "npm run build && npm start"
